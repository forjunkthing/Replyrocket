'use client';
import{useState}from'react';
import'../auth.css';
export default function Login(){const[email,setEmail]=useState(''),[sent,setSent]=useState(false);function submit(e){e.preventDefault();localStorage.setItem('replyrocket-user',JSON.stringify({email,mode:'demo'}));setSent(true)}return <main className="authPage"><a className="authBrand" href="/"><span>↗</span>ReplyRocket</a><section className="authCard"><p className="eyebrow">WELCOME BACK</p><h1>Sign in to ReplyRocket</h1><p>Use your work email to open your review workspace.</p>{sent?<div className="authSuccess"><strong>Demo account ready</strong><p>Real passwordless authentication will activate when Supabase credentials are added.</p><a className="primary authButton" href="/onboarding">Continue</a></div>:<form onSubmit={submit}><label>Email address<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@business.co.uk"/></label><button className="primary authButton">Continue with email</button><div className="authDivider"><span>or</span></div><button type="button" className="googleButton" onClick={()=>{localStorage.setItem('replyrocket-user',JSON.stringify({email:'google-demo@replyrocket.app',mode:'demo'}));location.href='/onboarding'}}>Continue with Google</button><small>By continuing, you agree to the terms and privacy policy.</small></form>}</section></main>